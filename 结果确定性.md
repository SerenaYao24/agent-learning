- AI在生产环境中的不确定性（如偷懒、格式错误、输出中断）是开发复杂度的核心来源。
- 传统API调用模式下，开发者需在运行时层处理分段、重试、校验等大量“擦屁股”工作。
- AI系统可分为模型层、协议层、运行时层和契约层，其中运行时层最耗时但难以复用。
  - 模型层：我们是用Claude还是GPT？用Opus还是Haiku？用什么Reasoning Effort？
  - 协议层：定义了模型与外部系统的交互规范，如API调用、数据格式等。用Chat Completion API还是Response API？用MCP还是RESTful API？Rate Limit怎么解决？JSON Mode要不要开启？当我们说调用API的时候，大多数情况下我们指的是协议层。
  - 运行时层：状态怎么管理？工具怎么调用？文件的内容怎么给AI？权限怎么控制？用多少并发？这一层不是传统意义调用API的开发内容，但是但凡想要把AI稳定用到生产环境，这是绕不过去的一层。
  - 契约层：到底什么样的标准算成功？比如拿到AI的结果之后做什么检查？Guardrail怎么设？什么时候要引入人工干预？怎么保证不违反社会主义核心价值观？这一层决定了我们能不能信任AI的输出，并且真的把它用于生产。
- 不同模型具有不同failure pattern，使运行时层：通过文件系统和工具调用，使AI具备“执行-观测-纠错”的闭环能力。
- evaluation-first方法要求明确定义验收标准，让AI自主判断任务是否完成并持续修正。
- 从“过程确定性”转向“结果确定性”，利用低成本token换取反复验证与正确结果。
- 这种模式提升了系统上限，因为无需穷举所有路径，只需定义“什么是对的”。
- Agent模式引入安全风险（如文件操作与代码执行），需通过权限控制与sandbox缓解。

如何设计高质量的AI任务验收标准以支持evaluation-first？
核心思路就一句话：把“什么叫完成任务”写成机器也能判断的规则，而不是人类主观感觉。
设计高质量验收标准的几个关键原则（都偏实操）：
1. 从“好不好”改成“对不对”，让程序或模型可以自动打分，而不是人拍脑袋
不要写：“翻译要自然流畅”“总结要清晰”
要写可判定的条件：是否覆盖原文所有段落（逐段对齐）、是否存在未翻译片段（中文残留检测）、是否术语一致（术语表匹配）、是否符合指定格式（JSON schema / markdown结构）
2. 拆成多个独立检查（而不是一个总评价）
不要只有一个“总体评分”，而是拆成：完整性（有没有漏）、正确性（有没有错）、一致性（术语/风格）、格式合规（结构/标点/编码）、每一项都能单独失败、单独修复。
3. 设计“失败信号”，而不是只定义成功信号：什么情况算失败？怎么检测失败？
evaluation-first本质是：让错误可以被稳定识别
4. 验收标准要“可驱动修复”，不仅要能判错，还要指向怎么改。
5. 数量适中，5～10条核心标准最有效，覆盖80%错误场景即可
6. 引入参考物，用“对比/基线”提升稳定性（进阶）：和原文对齐（长度/结构）、和历史版本对比（diff）、和示例输出对齐（few-shot），比纯规则更鲁棒
8. 把验收标准前置

我是仿真测试平台的产品，现在用 claude code 生成界面效果图。那我现在要写任务验收标准，你协助我一下。
开始一个设计任务的输入有：文字版的需求文档（结构化一般，可能有细节功能缺失）、已有设计 demo（数个，未抽象出设计手册/规则）
我的 evaluation rule：
1. 设计风格一致性（Design Consistency）
必须满足以下检查项：
- 颜色：
  - 主色、辅助色必须来自 demo1/demo2 的色板（禁止新增主色）
- 字体：
  - 字号层级（标题/正文/说明）必须与 demo 对齐（允许±1级）
- 组件：
  - 按钮 / 表单 / 表格 / 卡片样式必须复用已有样式（不可新造组件）
- 布局：
  - 页面结构需符合已有模式（如：顶部导航 + 左侧菜单 + 主内容区）  
👉 ✅ 验收方式：
“是否出现 demo 中不存在的新视觉体系？”（是=不通过）
2. 功能完整性（Functional Coverage）
需求文档中所有功能点必须在界面中有对应表达：

✅ 有入口（按钮/菜单/触发点）
✅ 有状态（loading / 空状态 / 错误态）
✅ 有反馈（成功/失败提示）
不允许：合并功能、隐藏功能、用“默认行为”替代明确功能
👉 ✅ 验收方式：
“需求点 → UI 映射表是否100%覆盖”（是=通过）
3. 非功能需求映射（Non-functional Mapping）
把“非功能需求”强制落到 UI：
例如：
性能：必须有 loading / skeleton
实时性：必须有刷新机制 / 状态提示
精度/数据：必须有单位 / 精度展示
👉 ✅ 验收方式：
“每个非功能需求是否有对应 UI 表达？”
4. 交互完整性（Interaction Completeness） ←（你现在缺的重点）
每个功能必须包含：
初始态
操作中（loading）
成功态
失败态
空数据态
👉 ✅ 验收方式：
“是否存在只画‘静态界面’但没有状态设计？”

5. 可实现性约束（Feasibility）
不允许：不可实现的动画、不明确的数据来源、不合理的交互（比如无触发路径）
👉 ✅ 验收方式：
“前端是否可以直接按图实现？”

6. 改动边界控制（Change Boundary）
只允许：在现有设计体系内扩展
不允许：重做设计语言、改整体布局范式
👉 ✅ 验收方式：
“是否改变了设计系统而不是复用？”